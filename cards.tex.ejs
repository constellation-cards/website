% !TeX program = xelatex

% Needed for Tikz to work under xelatex
\def\pgfsysdriver{pgfsys-dvipdfm.def}

\documentclass{article}
\usepackage[paperwidth=3in, paperheight=5in, margin=3mm]{geometry} % Print on 3x5 cards

%%% Fonts
\usepackage{fontspec}
\defaultfontfeatures{Scale=MatchLowercase}
\usepackage{microtype}
\UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
\usepackage{parskip}
\setlength{\emergencystretch}{3em}  % prevent overfull lines

%%% Sections
\newfontfamily\cardheaderfont[Path = fonts/, SizeFeatures={Size=20}]{NotoSerif-Bold} % Serif header
\usepackage{sectsty}
\usepackage[normalem]{ulem} % Support underlines in sections
\setcounter{secnumdepth}{0} % Sections are not numbered
\sectionfont{\cardheaderfont\centering\underline} % Sections are centered

%%% Body
\newfontfamily\cardbodyfont[Path = fonts/, SizeFeatures={Size=14}, ItalicFont={Lato-Italic}]{Lato-Regular}
% Allows emphasis in card text e.g. {\cardbodyemph{"Ow, that bloody well hurt!" -- Tana}}

%%% Images
\usepackage{graphicx}
\usepackage{tikzpagenodes}

%%% Prompts
\usepackage{enumitem}
\newfontfamily\cardpromptfont[Path = fonts/, SizeFeatures={Size=12}]{Lato-Regular}
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}} % Support for bulleted lists

%%% Rules
\usepackage{changepage}
\newfontfamily\cardrulefont[Path = fonts/, SizeFeatures={Size=10}]{Lato-Italic}

% Document
\begin{document}
<% for (let card of cards) { %>
<%- card %>
\clearpage
<% } %>
\end{document}
